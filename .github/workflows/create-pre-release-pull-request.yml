name: Create beta Release Branch

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      base_branch:
        description: 'Branch to create beta release from'
        required: true
        default: 'main'
      bump_type:
        description: 'Version bump type (major, minor, patch)'
        required: true
        default: 'patch'
        type: choice
        options:
          - major
          - minor
          - patch

jobs:
  create-beta-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.base_branch }}

      - name: Check if merge is from beta branch
        id: check_beta
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "Manual run: not skipping."
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "is_beta=false" >> $GITHUB_OUTPUT
            echo "clean_branch=manual" >> $GITHUB_OUTPUT
            exit 0
          fi
          COMMIT_MSG=$(git log -1 --pretty=%B)
          SRC_BRANCH=$(echo "$COMMIT_MSG" | grep -oE 'from [^ ]+' | awk '{print $2}')
          CLEAN_BRANCH=$(basename "$SRC_BRANCH")
          echo "Source branch: $CLEAN_BRANCH"
          echo "clean_branch=$CLEAN_BRANCH" >> $GITHUB_OUTPUT
          if [[ -z "$CLEAN_BRANCH" ]] || [[ "$CLEAN_BRANCH" != *beta* ]]; then
            echo "Not a merge from a beta branch. Skipping workflow."
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "is_beta=false" >> $GITHUB_OUTPUT
            exit 0
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "is_beta=true" >> $GITHUB_OUTPUT
          fi

      - name: Extract bump type from source branch or input
        if: steps.check_beta.outputs.skip == 'false'
        id: bump_type
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "bump_type=${{ github.event.inputs.bump_type }}" >> $GITHUB_OUTPUT
          else
            COMMIT_MSG=$(git log -1 --pretty=%B)
            SRC_BRANCH=$(echo "$COMMIT_MSG" | grep -oE 'from [^ ]+' | awk '{print $2}')
            CLEAN_BRANCH=$(basename "$SRC_BRANCH")
            BUMP_TYPE=$(echo "$CLEAN_BRANCH" | grep -oE '(patch|minor|major)' || echo "patch")
            echo "Bump type: $BUMP_TYPE"
            echo "bump_type=$BUMP_TYPE" >> $GITHUB_OUTPUT
          fi

      - name: Get current version
        if: steps.check_beta.outputs.skip == 'false'
        id: current_version
        run: |
          CURRENT_VERSION=$(grep -Po '^version = "\K[^"]+' pyproject.toml | head -1)
          echo "Extracted version: $CURRENT_VERSION"
          if [[ -z "$CURRENT_VERSION" ]]; then
            echo "Error: Could not extract version from pyproject.toml"
            exit 1
          fi
          echo "version=$CURRENT_VERSION" >> $GITHUB_OUTPUT

      - name: Bump version
        if: steps.check_beta.outputs.skip == 'false'
        id: bump_version
        run: |
          BUMP_TYPE="${{ steps.bump_type.outputs.bump_type }}"
          echo "Bump type: $BUMP_TYPE"
          CURRENT_VERSION="${{ steps.current_version.outputs.version }}"
          # Extract numeric part and suffix
          NUMERIC_VERSION=$(echo "$CURRENT_VERSION" | grep -oE '^[0-9]+\.[0-9]+\.[0-9]+')
          SUFFIX=$(echo "$CURRENT_VERSION" | sed -E 's/^[0-9]+\.[0-9]+\.[0-9]+//')
          if [[ -z "$NUMERIC_VERSION" ]]; then
            echo "Error: Could not extract numeric version from $CURRENT_VERSION"
            exit 1
          fi
          IFS='.' read -r MAJOR MINOR PATCH <<< "$NUMERIC_VERSION"
          if [[ -z "$MAJOR" || -z "$MINOR" || -z "$PATCH" ]]; then
            echo "Error: Version must have three numeric parts"
            exit 1
          fi
          ORIG_VERSION="$NUMERIC_VERSION$SUFFIX"
          case "$BUMP_TYPE" in
            major)
              MAJOR=$((MAJOR + 1))
              MINOR=0
              PATCH=0
              ;;
            minor)
              MINOR=$((MINOR + 1))
              PATCH=0
              ;;
            patch)
              PATCH=$((PATCH + 1))
              ;;
          esac
          NEW_VERSION="${MAJOR}.${MINOR}.${PATCH}${SUFFIX}"
          # Only check if the new version is greater if there's no suffix (i.e., not a pre-release)
          if [[ -z "$SUFFIX" ]]; then
            if ! python -c "from packaging import version; assert version.parse('$NEW_VERSION') > version.parse('$CURRENT_VERSION'), 'New version must be greater than current version'"; then
              echo "Error: New version '$NEW_VERSION' is not greater than current version '$CURRENT_VERSION'"
              exit 1
            fi
          fi
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "orig_version=$ORIG_VERSION" >> $GITHUB_OUTPUT

      - name: Set branch name
        id: set_branch
        run: |
          echo "branch_name=beta-release-v${{ steps.bump_version.outputs.new_version }}" >> $GITHUB_OUTPUT

      - name: Create and checkout beta release branch
        run: |
          git checkout -b "${{ steps.set_branch.outputs.branch_name }}"

      - name: Update Changelog
        run: |
          NEW_VERSION="${{ steps.bump_version.outputs.new_version }}"
          # Prepare new changelog section
          {
            echo "## $NEW_VERSION (Beta)"
            echo ""
            git log $(git describe --tags --abbrev=0 2>/dev/null || git rev-list --max-parents=0 HEAD)..HEAD --pretty=format:"- %s"
            echo ""
          } > NEW_CHANGELOG.md

          # Insert after the first line (# Changelog)
          awk 'NR==1{print; system("cat NEW_CHANGELOG.md"); next} 1' CHANGELOG.md > CHANGELOG.md.tmp
          mv CHANGELOG.md.tmp CHANGELOG.md
          rm NEW_CHANGELOG.md

      - name: Update pyproject.toml version
        if: steps.check_beta.outputs.skip == 'false'
        run: |
          ORIG_VERSION="${{ steps.bump_version.outputs.orig_version }}"
          NEW_VERSION="${{ steps.bump_version.outputs.new_version }}"
          BUMP_TYPE="${{ steps.bump_type.outputs.bump_type }}"
          # If this is a beta branch, append 'beta'
          if [[ "$BUMP_TYPE" == "beta" ]]; then
            NEW_VERSION="${NEW_VERSION}beta"
          fi
          sed -i "s/version = \".*\"/version = \"$NEW_VERSION\"/" pyproject.toml

      - name: Commit Changelog and Version
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add CHANGELOG.md pyproject.toml
          git commit -m "Update CHANGELOG.md and pyproject.toml for version ${{ steps.bump_version.outputs.new_version }}"
          git push origin "beta-release-v${{ steps.bump_version.outputs.new_version }}"
  
      - name: push beta release branch
        run: |
          NEW_VERSION="${{ steps.bump_version.outputs.new_version }}"
          BRANCH_NAME="beta-release-v$NEW_VERSION"
          git push origin "$BRANCH_NAME"

      - name: Create PR on GitHub
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          NEW_VERSION="${{ steps.bump_version.outputs.new_version }}"
          ORIG_VERSION="${{ steps.current_version.outputs.version }}"
          
          # Read the PR template and only replace the unique placeholder
          PR_BODY=$(cat .github/PULL_REQUEST_TEMPLATE.md | sed "s/{{RELEASE_SUMMARY}}/Release version bump from v$ORIG_VERSION to v$NEW_VERSION/")
          
          gh pr create \
            --title "Release v$NEW_VERSION" \
            --body "$PR_BODY" \
            --base main \
            --head "beta-release-v$NEW_VERSION" \
            --draft

      - name: Validate beta branch name
        if: steps.check_beta.outputs.skip == 'false' && steps.check_beta.outputs.clean_branch != 'manual'
        run: |
          CLEAN_BRANCH="${{ steps.check_beta.outputs.clean_branch }}"
          if [[ ! "$CLEAN_BRANCH" =~ ^beta-release-v[0-9]+\.[0-9]+\.[0-9]+ ]]; then
            echo "Error: Beta branch name must be in the format beta-release-vX.Y.Z"
            exit 1
          fi
            